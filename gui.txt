Renderer.labelRenderer {
    TextColor = #3C3C3C;
}

Renderer.valueDisplayRenderer {
    TextColor = #111111;
    TextStyle = Bold;
}

ChildWindow.controlsPanel {
    Size = (245, 140);
    Position = (5, parent.height - height - 5);
    Title = "Time control";
    TitleButtons = None;

    Renderer = {
        BorderColor = #333333;
        Borders = 0;
        TitleBarColor = #999999;
        BackgroundColor = #999999;
        TitleColor = #000;
    }

    Label.timeSpeedLabel {
        ScrollbarPolicy = Never;
        Position = (10, 10);
        Renderer = &labelRenderer;
        Size = (100, 18);
        Text = "Time speed";
        TextSize = 13;
    }

    Label.timeSecondsLabel {
        ScrollbarPolicy = Never;
        Position = (timeSpeedLabel.left, timeSpeedLabel.bottom + 1);
        Renderer = &labelRenderer;
        Size = (100, 18);
        Text = "Time (sec)";
        TextSize = 13;
    }

    Label.timeStepsLabel {
        ScrollbarPolicy = Never;
        Position = (timeSecondsLabel.left, timeSecondsLabel.bottom + 1);
        Renderer = &labelRenderer;
        Size = (100, 18);
        Text = "Time (steps)";
        TextSize = 13;
    }

    Button.stepBackButton {
        Position = (pauseButton.left - width - 2, pauseButton.top);
        TextSize = 20;
        Renderer = {
            Texture = "resources/gui/previous.png";
            TextureHover = "resources/gui/previous_hover.png";
        }
        Size = (40, 40);
    }

    Button.pauseButton {
        Size = (40, 40);
        Position = ((parent.width - width) / 2, parent.height - height - 28);
        TextSize = 20;
        Renderer = {
            Texture = "resources/gui/pause.png";
            TextureHover = "resources/gui/pause_hover.png";
        }
    }

    Button.stepForwardButton {
        Position = (pauseButton.right + 2, pauseButton.top);
        TextSize = 20;
        Renderer = {
            Texture = "resources/gui/next.png";
            TextureHover = "resources/gui/next_hover.png";
        }
        Size = (40, 40);
    }


    Label.timeSecondsDisplay {
        ScrollbarPolicy = Never;
        Position = (timeSecondsLabel.right, timeSecondsLabel.top);
        Renderer = &valueDisplayRenderer;
        Size = (100, 18);
        Text = "0";
        TextSize = 13;
    }

    Label.timeStepsDisplay {
        ScrollbarPolicy = Never;
        Position = (timeStepsLabel.right, timeStepsLabel.top);
        Renderer = &valueDisplayRenderer;
        Size = (100, 18);
        Text = "0";
        TextSize = 13;
    }
}
